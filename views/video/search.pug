extends ../layout/main
include ../mixin/videoBlock


block content
    .search_header
        h3 검색어는 #{searchingBy}
    .search_videos
        each video in videos
            +videoBlock({
                id:video.id,
                title:video.title,
                views:video.views,
                videoFile:video.videoFile
            })