header.header
    .header_column
        a(href=routes.home)
            i.fab.fa-youtube #{siteName}
    .header_column
        form.frm(action=routes.search, method="get")
            input(type="text", placeholder="검색어를 입력해주세요.", name="searchWrd")
    .header_column
        ul
            if !user.isAuthenticated
                li
                    a(href=routes.join) 회원가입
                li
                    a(href=routes.login) 로그인
            else
                li
                    a(href=routes.upload) 업로드
                li
                    a(href=routes.user_detail(user.id)) 프로필
                li
                    a(href=routes.logout) 로그아웃